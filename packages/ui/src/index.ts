/**
 * @ejo/ui
 * Shared design-system components used by both apps/website and
 * apps/portal, so the two experiences stay visually consistent.
 * Phase 1 ships only the primitives the skeleton pages need; the
 * full component set (Table, Modal, Tabs, etc. per the design-system
 * spec) is added incrementally in later phases.
 */
export { ComingSoon } from './ComingSoon';
export { ModuleStatusBadge } from './ModuleStatusBadge';
