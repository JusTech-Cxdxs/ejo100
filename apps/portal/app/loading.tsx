import { EjoLoader } from '@/components/EjoLoader';

/**
 * Next.js App Router automatic loading UI — shown while any route segment
 * is loading. See components/EjoLoader.tsx for the animation itself and
 * its configuration.
 */
export default function Loading() {
  return <EjoLoader />;
}
