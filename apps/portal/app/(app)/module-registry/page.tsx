import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function Page() {
  return <PagePlaceholder title="Module Registry" description="Platform-wide admin view of every module's status (Live, Coming Soon, Disabled) per company." />;
}
