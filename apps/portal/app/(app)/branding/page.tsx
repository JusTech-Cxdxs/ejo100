import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function Page() {
  return <PagePlaceholder title="Branding Engine" description="Configure each organisation's logo, colours and typography — powers the dynamic branding engine." />;
}
