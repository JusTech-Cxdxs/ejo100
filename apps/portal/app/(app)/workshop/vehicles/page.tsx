import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function WorkshopVehiclesPage() {
  return (
    <PagePlaceholder
      title="Vehicle Registry"
      description="Vehicle registration, chassis/VIN, plate number, model and full service history. Arrives in Phase 4 (Workshop Module)."
    />
  );
}
