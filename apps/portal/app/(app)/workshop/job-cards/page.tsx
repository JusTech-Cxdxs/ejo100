import { PagePlaceholder } from '@/components/PagePlaceholder';

export default function WorkshopJobCardsPage() {
  return (
    <PagePlaceholder
      title="Job Cards"
      description="Case files linking Job Card → Estimate → Customer Approval → Parts Request → Road Test → Invoice. Workflow engine and case-file logic arrive in Phase 4 (Workshop Module)."
    />
  );
}
