import { Loader } from '@/components/Loader';

/**
 * Next.js App Router automatic loading UI — shown while any route segment
 * is loading. See components/Loader.tsx for the animation itself and its
 * configuration.
 */
export default function Loading() {
  return <Loader />;
}
