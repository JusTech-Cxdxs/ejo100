import { Injectable } from '@nestjs/common';

@Injectable()
export class DocumentsService {
  // Phase 1 stub — replace with @ejo/database (Prisma) queries when this
  // module's build phase is reached.
  findAll() {
    return [];
  }
}
